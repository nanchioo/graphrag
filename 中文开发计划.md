# GraphRAG 最小侵入式改造开发计划

## 1. 项目改造整体架构设计

### 1.1 改造目标

在 **不重构 GraphRAG 核心代码**、**不拆多服务**、**不引入复杂工程结构** 的前提下，将当前仓库扩展为一个可落地的“前后端分离知识图谱管理系统”，具备以下能力：

- 图谱项目管理
- 源文件上传与持久化
- 基于 GraphRAG 的切片与索引构建
- 图谱节点/关系查看
- AI 模型配置管理
- 基于 GraphRAG 查询能力的问答接口

### 1.2 总体方案

采用“**外挂式扩展层**”方案，不直接改 GraphRAG 核心流水线，而是在仓库根目录新增一个轻量 FastAPI 应用层：

- **前端**：`web/` 下新增 React + TypeScript 单页应用
- **后端**：根目录新增 `main.py` 和 `api/` 目录，对 GraphRAG 做 Router + Service 封装
- **数据层**：沿用 GraphRAG 现有 `settings.yaml + prompts + output + lancedb + parquet` 结构
- **项目管理层**：新增图谱项目注册表与系统配置文件，统一管理多个 GraphRAG 数据集目录

### 1.3 架构核心原则

1. **GraphRAG 只作为底层能力引擎使用**
   - 直接复用现有能力：
   - `graphrag.cli.initialize.initialize_project_at`
   - `graphrag.config.load_config.load_config`
   - `graphrag.api.build_index`
   - `graphrag.api.local_search / global_search / basic_search / drift_search`

2. **每个图谱项目对应一个标准 GraphRAG 工作目录**
   - 目录下保留 `settings.yaml`、`.env`、`prompts/`、`input/`、`output/`、`cache/`、`lancedb/`
   - 这样后续调试、迁移、脱离后台单独运行 GraphRAG 都成立

3. **上传层做归一化包装，不改 GraphRAG 底层数据结构**
   - 原始文件先保存到每个图谱项目的 `input/`
   - 构建时由外挂服务扫描 `input/`，归一化为 GraphRAG 需要的 `documents DataFrame`
   - 最终仍调用 `build_index(..., input_documents=...)`，GraphRAG 输出结构保持不变

4. **模型配置由后台管理，构建/查询前同步到 GraphRAG 项目配置**
   - 模型配置持久化在 `config/app_settings.json`
   - 每次构建/查询前，将选中的模型 profile 映射到目标图谱项目的 `settings.yaml` / `.env`

5. **后端单命令启动**
   - 目标启动方式：`uvicorn main:app --reload`
   - 若 `web/dist` 已存在，则由 FastAPI 一并托管前端静态资源

### 1.4 推荐运行形态

```text
React Web UI
    ↓ HTTP
FastAPI (main.py)
    ↓ Router
api/services/*
    ↓ Wrapper
GraphRAG API / Config / Query
    ↓
data/projects/<graph_id>/*
```

### 1.5 关键实现假设

- **本期配置持久化优先用 JSON，而不是 SQLite**
  - 原因：依赖更少，改造更轻
- **本期优先支持单机本地运行**
  - 不引入任务队列、中间件、消息总线
- **本期允许图谱构建为后台异步任务**
  - 避免索引请求长时间阻塞 HTTP
- **本期支持 OpenAI / Azure / Ollama，但 Ollama 需满足 GraphRAG 对结构化输出和 embedding 的要求**
  - 若本地模型不稳定返回 JSON/schema，索引质量和查询稳定性会受影响

---

## 2. 模块拆分说明

### 2.1 后端模块

#### `main.py`

- FastAPI 应用入口
- 挂载三类路由：
  - `/api/graph`
  - `/api/config`
  - `/api/query`
- 可选挂载 `web/dist` 为前端静态资源

#### `api/routers/graph.py`

- 图谱项目增删查
- 文件上传
- 触发构建
- 删除切片/索引产物
- 图谱结构查看

#### `api/routers/config.py`

- 模型配置管理
- 上传路径/项目根路径配置管理
- 默认图谱设置管理

#### `api/routers/query.py`

- 问答接口 `/api/query` 或 `/api/chat`
- 查询模式切换（local / global / basic / drift）

#### `api/services/app_config_service.py`

- 读写系统级配置
- 管理模型 profile
- 统一做 `api_key` 脱敏返回

#### `api/services/graph_registry_service.py`

- 管理图谱项目注册表
- 保存图谱基础元数据：
  - `id`
  - `name`
  - `root_dir`
  - `status`
  - `model_profile_id`
  - `created_at`
  - `last_build_at`

#### `api/services/project_workspace_service.py`

- 创建标准 GraphRAG 项目目录
- 调用 `initialize_project_at(...)`
- 确保每个图谱工作目录完整可用

#### `api/services/source_ingest_service.py`

- 保存原始上传文件到 `input/`
- 按扩展名归一化为 GraphRAG 需要的 document 结构
- 支持：
  - `.txt`
  - `.md`
  - `.json`
  - `.jsonl`
  - `.csv`
  - `.pdf`
  - 其他 `markitdown` 能处理的文档类格式

#### `api/services/graphrag_wrapper_service.py`

- 负责将系统配置映射到 GraphRAG 项目配置
- 负责调用 `load_config(...)`
- 负责调用 `build_index(...)`
- 负责读取 `output/*` 结果表

#### `api/services/graph_view_service.py`

- 从 `entities`、`relationships`、`communities`、`community_reports` 读取图谱数据
- 转为前端可直接渲染的 `nodes / edges` 结构
- 做简单裁剪，避免大图直接压垮页面

#### `api/services/query_service.py`

- 封装 GraphRAG 查询调用
- 支持 `local/global/basic/drift`
- 输出统一响应结构：
  - `answer`
  - `mode`
  - `context`
  - `citations`

### 2.2 前端模块

#### 图谱管理页

- 图谱列表
- 新建图谱
- 上传文件
- 触发构建
- 删除图谱
- 删除索引/切片产物
- 查看图谱结构

#### 模型配置页

- 模型 profile 列表
- 新增/编辑/删除 profile
- 设置默认 profile
- 配置存储路径

#### 问答页

- 选择图谱
- 选择查询模式
- 输入问题
- 展示回答和上下文

---

## 3. 目录结构设计（基于原项目）

建议在现有仓库内新增如下目录，保持单仓库、单项目结构：

```text
graphrag/
├─ main.py
├─ api/
│  ├─ __init__.py
│  ├─ deps.py
│  ├─ routers/
│  │  ├─ __init__.py
│  │  ├─ graph.py
│  │  ├─ config.py
│  │  └─ query.py
│  ├─ schemas/
│  │  ├─ __init__.py
│  │  ├─ common.py
│  │  ├─ graph.py
│  │  ├─ config.py
│  │  └─ query.py
│  └─ services/
│     ├─ __init__.py
│     ├─ app_config_service.py
│     ├─ graph_registry_service.py
│     ├─ project_workspace_service.py
│     ├─ source_ingest_service.py
│     ├─ graphrag_wrapper_service.py
│     ├─ graph_view_service.py
│     └─ query_service.py
├─ config/
│  ├─ app_settings.json
│  └─ graph_registry.json
├─ data/
│  └─ projects/
│     └─ <graph_id>/
│        ├─ settings.yaml
│        ├─ .env
│        ├─ prompts/
│        ├─ input/
│        ├─ output/
│        ├─ cache/
│        ├─ reports/
│        └─ lancedb/
├─ web/
│  ├─ package.json
│  ├─ tsconfig.json
│  ├─ vite.config.ts
│  ├─ index.html
│  └─ src/
│     ├─ main.tsx
│     ├─ App.tsx
│     ├─ api/
│     │  └─ client.ts
│     ├─ pages/
│     │  ├─ GraphManagePage.tsx
│     │  ├─ ModelConfigPage.tsx
│     │  └─ QueryPage.tsx
│     ├─ components/
│     │  ├─ GraphTable.tsx
│     │  ├─ UploadPanel.tsx
│     │  ├─ BuildStatusCard.tsx
│     │  ├─ GraphPreview.tsx
│     │  ├─ ModelProfileForm.tsx
│     │  └─ QueryPanel.tsx
│     └─ types/
│        └─ index.ts
├─ packages/
│  └─ graphrag/
│     └─ graphrag/
│        └─ ...  # 原有核心逻辑保持不动
└─ 中文开发计划.md
```

### 目录设计说明

- `api/`、`config/`、`web/` 均在主项目根目录，符合“**不新建独立项目**”
- `data/projects/<graph_id>` 复用 unified-search-app 的“多数据集目录”思路，但由当前 FastAPI 后台统一接管
- `packages/graphrag/graphrag/**` 仍作为底层引擎，不在本期做结构性改造

---

## 4. 后端改造点（新增 / 修改 / 不动）

### 4.1 新增

| 类型 | 路径 | 作用 |
| --- | --- | --- |
| 新增 | `main.py` | FastAPI 启动入口 |
| 新增 | `api/routers/*` | 管理图谱、配置、问答接口 |
| 新增 | `api/services/*` | 对 GraphRAG 做 service 包装 |
| 新增 | `api/schemas/*` | 请求/响应模型 |
| 新增 | `config/app_settings.json` | 系统级配置持久化 |
| 新增 | `config/graph_registry.json` | 图谱项目注册表 |
| 新增 | `data/projects/*` | 每个图谱的 GraphRAG 工作目录 |
| 新增 | `web/*` | React + TS 前端 |

### 4.2 修改

| 类型 | 路径 | 修改内容 |
| --- | --- | --- |
| 修改 | `pyproject.toml` | 增加 FastAPI 运行所需依赖和开发脚本 |
| 修改 | 可能的 `README.md` | 增补“如何启动后台/前端”的最小说明 |

### 4.3 不动

以下区域本期原则上 **不改或只读调用**：

| 路径 | 原因 |
| --- | --- |
| `packages/graphrag/graphrag/api/*` | 直接复用现有索引/查询 API |
| `packages/graphrag/graphrag/cli/*` | 保留 CLI 行为，不改原有入口 |
| `packages/graphrag/graphrag/index/*` | 不动索引流水线 |
| `packages/graphrag/graphrag/query/*` | 不动查询引擎 |
| `packages/graphrag-input/*` | 不重构 GraphRAG 输入层 |
| `packages/graphrag-storage/*` | 沿用现有输出与存储机制 |
| `packages/graphrag-vectors/*` | 沿用现有向量存储结构 |

### 4.4 为什么这是“最小侵入式”

- 不替换 GraphRAG CLI / API
- 不改变 GraphRAG 输出表结构
- 不改变 GraphRAG 查询算法
- 不拆独立 Python 服务
- 不做 monorepo / workspace 重构
- 所有新增代码都集中在根目录扩展层

---

## 5. 前端设计方案

### 5.1 前端技术选型

- `React + TypeScript`
- `Vite`
- `Ant Design`
- 图谱可视化采用 `ECharts graph` 即可，避免引入过重的图谱前端框架

### 5.2 页面设计

#### 页面一：图谱管理页

主要功能：

- 图谱列表展示
- 新建图谱
- 选择图谱查看详情
- 上传源文件
- 触发构建
- 删除图谱
- 删除切片/索引数据
- 图谱节点/关系预览

建议布局：

- 左侧：图谱列表
- 右侧：详情页签
  - 基本信息
  - 源文件
  - 构建状态
  - 图谱预览

#### 页面二：模型配置页

主要功能：

- 查看已有模型 profile
- 新增/编辑 profile
- 选择默认 profile
- 配置图谱根路径/上传路径

字段建议：

- `provider`：`openai | azure | ollama`
- `name`
- `base_url`
- `api_key`
- `model_name`
- `embedding_model_name`（建议补充，因 GraphRAG 索引需要 embedding）
- `api_version`（Azure 可选）
- `is_default`

> 说明：用户要求的核心字段是 `base_url / api_key / model_name`，但为了让 GraphRAG 能稳定完成索引，建议增加 `embedding_model_name` 作为“可选高级字段”。

#### 页面三：问答页

主要功能：

- 选择图谱
- 选择查询模式
- 输入问题
- 展示回答
- 展示上下文片段/引用来源

建议布局：

- 顶部筛选栏：图谱、查询模式、community level
- 中部：问答输入框
- 下部：回答卡片 + 上下文折叠面板

### 5.3 前端交互原则

- 不做复杂状态管理，优先 `fetch + 局部 state`
- 不上 Redux
- 上传、构建、删除均用 `Modal + Message + Spin`
- 构建状态用 `Tag + Progress` 即可
- 图谱预览默认做限量展示，例如：
  - 节点最多 100 个
  - 边最多 200 条

---

## 6. API 设计（接口列表）

### 6.1 图谱管理接口 `/api/graph`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/graph` | 获取图谱列表 |
| `POST` | `/api/graph` | 新建图谱项目 |
| `GET` | `/api/graph/{graph_id}` | 获取图谱详情 |
| `POST` | `/api/graph/{graph_id}/files` | 上传源文件 |
| `GET` | `/api/graph/{graph_id}/files` | 获取源文件列表 |
| `POST` | `/api/graph/{graph_id}/build` | 触发 GraphRAG 构建 |
| `GET` | `/api/graph/{graph_id}/status` | 获取构建状态 |
| `DELETE` | `/api/graph/{graph_id}/artifacts` | 删除切片/索引/向量产物，保留源文件 |
| `DELETE` | `/api/graph/{graph_id}` | 删除整个图谱项目 |
| `GET` | `/api/graph/{graph_id}/graph` | 获取图谱节点关系预览 |
| `GET` | `/api/graph/{graph_id}/reports` | 获取 community/report 概览 |

#### 关键请求示例

`POST /api/graph`

```json
{
  "name": "customer-service-kg",
  "description": "客服知识库",
  "model_profile_id": "openai-default"
}
```

`POST /api/graph/{graph_id}/build`

```json
{
  "method": "standard",
  "force_rebuild": true
}
```

### 6.2 配置接口 `/api/config`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/config/system` | 获取系统配置 |
| `PUT` | `/api/config/system` | 更新系统配置 |
| `GET` | `/api/config/models` | 获取模型 profile 列表 |
| `POST` | `/api/config/models` | 新增模型 profile |
| `PUT` | `/api/config/models/{profile_id}` | 修改模型 profile |
| `DELETE` | `/api/config/models/{profile_id}` | 删除模型 profile |

#### 系统配置建议内容

```json
{
  "projects_root": "data/projects",
  "upload_root": "data/projects",
  "default_model_profile_id": "openai-default"
}
```

### 6.3 问答接口 `/api/query`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/query` | 发起 GraphRAG 问答 |
| `POST` | `/api/chat` | `/api/query` 别名，兼容前端语义 |

#### 请求结构建议

```json
{
  "graph_id": "customer-service-kg",
  "question": "退款流程有哪些关键风险点？",
  "mode": "local",
  "community_level": 2,
  "response_type": "Multiple Paragraphs"
}
```

#### 返回结构建议

```json
{
  "graph_id": "customer-service-kg",
  "mode": "local",
  "answer": "......",
  "context": {
    "entities": [],
    "relationships": [],
    "reports": [],
    "sources": []
  }
}
```

### 6.4 接口设计说明

- 所有接口统一返回：
  - `success`
  - `message`
  - `data`
- 构建接口建议返回 `job_id` 或最少返回 `status=running`
- `api_key` 不回显明文，接口返回时只给：
  - `has_api_key: true`
  - 或 `api_key_masked: "****abcd"`

---

## 7. 数据流说明

### 7.1 图谱创建数据流

1. 前端发起“新建图谱”
2. 后端在 `data/projects/<graph_id>/` 创建目录
3. 调用 `initialize_project_at(...)` 生成 GraphRAG 标准骨架
4. 在 `graph_registry.json` 中登记该图谱
5. 将默认模型 profile 同步到该图谱项目配置

### 7.2 文件上传数据流

1. 前端上传文本、JSON、Markdown、PDF 等文件
2. 后端保存原始文件到 `data/projects/<graph_id>/input/`
3. 后端记录文件元数据到注册表或图谱详情中
4. 不直接修改 GraphRAG 核心输入层

### 7.3 构建数据流

1. 前端点击“开始构建”
2. 后端读取该图谱绑定的模型 profile
3. 将模型参数同步到该图谱的 `settings.yaml` / `.env`
4. `source_ingest_service` 扫描 `input/` 下原始文件
5. 将不同类型文件归一化成统一 `documents DataFrame`
6. `graphrag_wrapper_service` 调用：
   - `load_config(root_dir=project_root)`
   - `build_index(config, input_documents=documents_df)`
7. GraphRAG 将结果写入：
   - `output/`
   - `cache/`
   - `lancedb/`
   - `reports/`
8. 后端更新构建状态为成功或失败

### 7.4 图谱查看数据流

1. 前端请求 `/api/graph/{graph_id}/graph`
2. 后端读取：
   - `output/entities`
   - `output/relationships`
3. 组装为：
   - `nodes`
   - `edges`
4. 前端用 ECharts 渲染简版关系图

### 7.5 问答数据流

1. 前端提交问题到 `/api/query`
2. 后端读取目标图谱项目配置和输出表
3. 根据 `mode` 选择：
   - `local_search`
   - `global_search`
   - `basic_search`
   - `drift_search`
4. 返回回答文本和上下文信息
5. 前端展示答案，并可折叠查看来源上下文

### 7.6 删除数据流

#### 删除切片/索引产物

- 删除：
  - `output/`
  - `cache/`
  - `reports/`
  - `lancedb/`
  - `update_output/`（若存在）
- 保留：
  - `settings.yaml`
  - `.env`
  - `prompts/`
  - `input/`

#### 删除整个图谱

- 删除整个 `data/projects/<graph_id>/`
- 同步删除 `graph_registry.json` 中的注册信息

> 删除实现需严格校验目标路径必须位于 `data/projects/` 内，避免误删。

---

## 8. 分步骤开发计划

### Step 1：搭建后端骨架

目标：

- 新增 `main.py`
- 新增 `api/routers`、`api/services`、`api/schemas`
- 跑通最基础 FastAPI 应用
- 约定统一响应格式

产出：

- `uvicorn main:app --reload` 可启动
- `/api/health` 或根接口可访问

### Step 2：实现系统配置与模型配置持久化

目标：

- 实现 `config/app_settings.json`
- 完成模型 profile 的增删改查
- 完成 `api_key` 脱敏输出
- 支持默认模型 profile

产出：

- `/api/config/system`
- `/api/config/models`

### Step 3：实现图谱项目注册表与工作目录初始化

目标：

- 实现 `graph_registry.json`
- 新建图谱时自动创建 `data/projects/<graph_id>`
- 调用 `initialize_project_at(...)` 完成 GraphRAG 项目骨架初始化

产出：

- `/api/graph`
- `/api/graph/{graph_id}`

### Step 4：实现文件上传与源文件归一化

目标：

- 支持上传多文件
- 原始文件落盘到 `input/`
- 构建前按扩展名归一化文档
- 支持 `txt/md/json/jsonl/csv/pdf`

产出：

- `/api/graph/{graph_id}/files`
- `source_ingest_service.py`

### Step 5：实现构建与清理能力

目标：

- 触发 GraphRAG 构建
- 支持异步构建状态追踪
- 支持删除切片/索引产物

产出：

- `/api/graph/{graph_id}/build`
- `/api/graph/{graph_id}/status`
- `/api/graph/{graph_id}/artifacts`

### Step 6：实现图谱查看与问答接口

目标：

- 从 GraphRAG 输出中读取实体、关系、社区报告
- 封装图谱预览接口
- 封装 `/api/query` / `/api/chat`

产出：

- `/api/graph/{graph_id}/graph`
- `/api/query`

### Step 7：实现 React 前端

目标：

- 初始化 `web/` 前端工程
- 完成三个页面：
  - 图谱管理页
  - 模型配置页
  - 问答页
- 完成 API 联调

产出：

- `web/src/pages/*`
- 可本地访问前端页面

### Step 8：接入静态托管与整体联调

目标：

- FastAPI 托管 `web/dist`
- 前后端一体运行
- 完成最小可用链路验证

联调链路：

1. 新建图谱
2. 配置模型
3. 上传文件
4. 触发构建
5. 查看节点关系
6. 发起问答
7. 删除切片数据
8. 删除图谱

### Step 9：补充最小验收标准

验收以“可运行”为准，不做额外工程化扩展：

- 后端可通过一个命令启动
- 可新增/删除图谱
- 可上传文件并落盘
- 可触发 GraphRAG 构建
- 可查看实体/关系简图
- 可配置 OpenAI / Azure / Ollama profile
- 可通过 `/api/query` 返回答案

---

## 结论

本方案的核心不是“重做 GraphRAG”，而是：

- 在仓库根目录增加一层 **轻量后台壳**
- 把 GraphRAG 当作 **索引与查询引擎**
- 用 **项目目录 + JSON 配置 + Router/Service 封装** 的方式完成后台化

这样可以同时满足：

- **最小侵入**
- **结构清晰**
- **前后端分离**
- **单命令启动**
- **后续可逐步扩展**

下一阶段建议按上述 Step 1 开始逐步落代码，每一步都严格标记：

- `✅ 已完成`
- `⏳ 进行中`

